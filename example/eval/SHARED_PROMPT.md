# Shared starting prompt (identical for all 3 cases)

Use this as the **first user message**. Do **not** paste a full acceptance checklist up front.

---

Build a **Todo** product in this empty project directory:

- **Backend** HTTP API for todos (create / list / update / delete / toggle done).
- **Frontend** in **React** that calls that API (not mock-only UI).
- Prefer **existing** libraries and frameworks that already exist in the world — do **not** reinvent wheels (routing, HTTP server, ORM/DB client, React tooling, etc.).
- Start from zero in the current working directory.
- Acceptance details are **not** fully specified yet: implement a sensible MVP, and when something is ambiguous or missing for a shippable result, **ask or adjust as you go** (same spirit as a real product chat).

Deliver something that can be run locally (how to start API + React, and a quick manual check that the UI talks to the API).

---

English paraphrase (same ask): Todo backend API + React frontend calling it; use real existing stacks; no full AC up front; discover gaps while building; runnable locally.

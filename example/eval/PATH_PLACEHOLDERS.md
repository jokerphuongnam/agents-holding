Path placeholders used in eval artifacts:
  $BENCH_V1 / $BENCH_V2 / $BENCH_V3  — local Todo bench worktrees
  $AGENTS_HOLDING                 — clone of this repo
  ~/.agents , ~/.grok , $USER     — machine-local

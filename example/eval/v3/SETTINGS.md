# Settings v3 (before runs)

| Case | Mode | Model | Effort | Company OS / plugin |
| --- | --- | --- | --- | --- |
| A | Plain vibe chat | grok-4.6 | medium | **No** |
| B | agents-holding Company OS | grok-4.6 (fallback 4.5) | low | **Yes** — create-company + company_os |

Shared product: Todo API + React + BE tests + FE unit/UI tests per `OUTPUT.md`.

Wall-clock: record start/end ISO per case. Tokens: from session `updates.jsonl` usage when available.

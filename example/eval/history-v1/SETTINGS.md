# Settings (recorded before runs)

| Case | Mode | Model | Effort / thinking | Company OS |
| --- | --- | --- | --- | --- |
| 1 | Chat only | **grok-4.6** | **medium**, prompt mode **full** | No |
| 2 | Naive subagents | **grok-4.6** | **medium**, prompt mode **full** | No |
| 3 | agents-holding end-to-end | **grok-4.5** | **low** | Yes — create-company → skills → implement |

Shared product: Todo **API + React** (+ backend tests + FE unit/UI tests per `EXPECTED.md`).

If the host cannot set effort separately from model, note **requested vs actual** in each case `report.md`.
